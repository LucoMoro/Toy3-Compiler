#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

#define BUFFER_SIZE  1024*4
char* buffer;


char* string_concat(char* s1, char* s2)
{
    char* ns = malloc(strlen(s1) + strlen(s2) + 1);
    strcpy(ns, s1);
    strcat(ns, s2);
    return ns;
}

char* int2str(int n)
{
    char buffer[BUFFER_SIZE];
    int len = sprintf(buffer,"%d",n);
    char *ns = malloc(len + 1);
    sprintf(ns,"%d",n);
    return ns;
}

char* char2str(char c)
{
    char *ns = malloc(2);
    sprintf(ns, "%c", c);
    return ns;
}

    char* double2str(double f)
    {
        char buffer[BUFFER_SIZE];
        int len = sprintf(buffer,"%f", f);
        char *ns = malloc(len + 1);
        sprintf(ns, "%f", f);
        return ns;
    }

char* bool2str(int b)
{
    char* ns = NULL;
    if(b)
    {
        ns = malloc(5);
        strcpy(ns, "true");
    }
    else
    {
        ns = malloc(6);
        strcpy(ns, "false");
    }
    return ns;
}

void fun_fun (char* *string1, int *n1, double *n2, char *character, int *boolean);
int somma_fun (int a, int b);


char* stringa1;
char* stringa2;
int numero_intero;
double numero_double;
int valore_booleano;
char* stringa;
char carattere;
void fun_fun (char* *string1, int *n1, double *n2, char *character, int *boolean) {
*string1=string_concat(*string1, char2str('g'));
*n1=*n1+100;
*n2=*n2+100;
*character='z';
*boolean=0;
}

int somma_fun (int a, int b) {
return a+b;
}



int main() {
int variabile = 3;
stringa1="pluto";
stringa2="paperino";
variabile=somma_fun(2, 3);
numero_intero=1;
numero_double=2;
valore_booleano=1;
stringa="ciao";
carattere='c';
printf("%s\n", "----------------------------------------");
printf("%s\n", string_concat(string_concat("CONCAT stringa: ", stringa1), stringa2));
printf("%s\n", string_concat(string_concat("CONCAT intero:", stringa1), int2str(1)));
printf("%s\n", string_concat(string_concat("CONCAT double:", stringa1), double2str(1.0)));
printf("%s\n", string_concat(string_concat("CONCAT char", stringa1), char2str('c')));
printf("%s\n", string_concat(string_concat("CONCAT bool", stringa1), bool2str(1)));
printf("%s\n", "----------------------------------------");
printf("%s\n", string_concat("valore prima numero intero:", int2str(numero_intero)));
printf("%s\n", string_concat("valore prima numero double:", double2str(numero_double)));
printf("%s\n", string_concat("valore prima stringa:", stringa));
printf("%s\n", string_concat("valore prima carattere:", char2str(carattere)));
printf("%s\n", string_concat("valore prima valore booleano:", bool2str(valore_booleano)));
printf("%s\n", "----------------------------------------");
fun_fun(&stringa, &numero_intero, &numero_double, &carattere, &valore_booleano);
printf("%s\n", string_concat("valore dopo numero intero:", int2str(numero_intero)));
printf("%s\n", string_concat("valore dopo numero double:", double2str(numero_double)));
printf("%s\n", string_concat("valore dopo stringa:", stringa));
printf("%s\n", string_concat("valore dopo carattere:", char2str(carattere)));
printf("%s\n", string_concat("valore dopo valore booleano:", bool2str(valore_booleano)));
printf("%s\n", "----------------------------------------");
printf("%s\n", string_concat("SOMMA: ", int2str(somma_fun(1, 2))));
printf("%s\n", string_concat("SOMMA SPECIALE: ", int2str(variabile)));
return 1; 
}
